# Origin of the ProxSync backend API. In development the Next dev server proxies
# /api/* to this origin so the browser talks to a single origin (matching the
# nginx deployment where /api/* and /* share a host).
PROXSYNC_API_ORIGIN=http://127.0.0.1:8000
